/**
 * Definition for singly-linked list.
 * function ListNode(val) {
 *     this.val = val;
 *     this.next = null;
 * }
 */

function ListNode(val) {
    this.val = val;
    this.next = null;
}

/**
 * @param {ListNode} l1
 * @param {ListNode} l2
 * @return {ListNode}
 */
var mergeTwoLists = function (l1, l2) {
    let newList = [];
    for (let i = 0; i < l1.val.length; i++) {

    }
};

console.assert(mergeTwoLists(new ListNode([1, 2]), [3, 4]) === [1, 3, 2, 4])